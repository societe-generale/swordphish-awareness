from django.urls import re_path
from Main import views

app_name = 'Main'

urlpatterns = [
    re_path(r'^$',
        views.index,
        name="index"),
    re_path(r'^$',
        views.campaign_overview,
        name="campaign_overview"),
    re_path(r'^targets$',
        views.campaign_targets,
        name="campaign_targets"),
    re_path(r'^campaigns/targets/createlist$',
        views.campaign_create_targets_list,
        name="campaign_create_targets_list"),
    re_path(r'^targets/deletelist/(?P<listid>\d+)$',
        views.campaign_delete_targets_list,
        name="campaign_delete_targets_list"),
    re_path(r'^targets/editlist/(?P<listid>\d+)$',
        views.campaign_edit_targets_list,
        name="campaign_edit_targets_list"),
    re_path(r'^targets/importtargerts/(?P<listid>\d+)$',
        views.campaign_import_targets_list,
        name="campaign_import_targets_list"),
    re_path(r'^targets/exporttargerts/(?P<listid>\d+)$',
        views.campaign_export_targets_list,
        name="campaign_export_targets_list"),
    re_path(r'^targets/createtarget/(?P<listid>\d+)$',
        views.campaign_create_target,
        name="campaign_create_target"),
    re_path(r'^targets/removetarget/(?P<listid>\d+)/(?P<targetid>\d+)$',
        views.campaign_delete_target,
        name="campaign_delete_target"),
    re_path(r'^targets/edittarget/(?P<listid>\d+)/(?P<targetid>\d+)$',
        views.campaign_edit_target,
        name="campaign_edit_target"),
    re_path(r'^targets/listtargets/(?P<listid>\d+)/(?P<page>\d+)?$',
        views.campaign_list_targets,
        name="campaign_list_targets"),
    re_path(r'^targets/lists/(?P<page>\d+)?$',
        views.campaign_list_targets_list,
        name="campaign_list_targets_list"),
    re_path(r'^templates$',
        views.campaign_templates,
        name="campaign_templates"),
    re_path(r'^templates/create/(?P<typeid>\d)(?:/(?P<duplicateid>\d+))?$',
        views.campaign_create_template,
        name="campaign_create_template"),
    re_path(r'^templates/edit/(?P<typeid>\d)/(?P<templateid>\d+)$',
        views.campaign_edit_template,
        name="campaign_edit_template"),
    re_path(r'^templates/delete/(?P<templateid>\d+)$',
        views.campaign_delete_template,
        name="campaign_delete_template"),
    re_path(r'^templates/view/(?P<templateid>\d+)$',
        views.campaign_view_template,
        name="campaign_view_template"),
    re_path(r'^templates/list/(?P<page>\d+)?$',
        views.campaign_list_template,
        name="campaign_list_template"),
    re_path(r'^campaigns$',
        views.campaign_campaigns,
        name="campaign_campaigns"),
    re_path(r'^campaigns/running$',
        views.campaign_running_campaigns,
        name="campaign_running_campaigns"),
    re_path(r'^campaigns/create/(?P<typeid>\d)(?:/(?P<duplicateid>\d+))?$',
        views.campaign_create_campaign,
        name="campaign_create_campaign"),
    re_path(r'^campaigns/edit/(?P<typeid>\d)/(?P<campaignid>\d+)$',
        views.campaign_edit_campaign,
        name="campaign_edit_campaign"),
    re_path(r'^campaigns/delete/(?P<campaignid>\d+)$',
        views.campaign_delete_campaign,
        name="campaign_delete_campaign"),
    re_path(r'^campaigns/test/(?P<campaignid>\d+)$',
        views.campaign_test_campaign,
        name="campaign_test_campaign"),
    re_path(r'^campaigns/download/(?P<campaignid>\d+)$',
        views.campaign_download_results,
        name="campaign_download_results"),
    re_path(r'^campaigns/dasbhoard/(?P<campaignid>\d+)$',
        views.campaign_display_dashboard,
        name="campaign_display_dashboard"),
    re_path(r'^campaigns/reportids/(?P<campaignid>\d+)$',
        views.campaign_submit_reported_ids,
        name="campaign_submit_reported_ids"),
    re_path(r'^campaigns/list/(?P<page>\d+)?$',
        views.campaign_list_campaigns,
        name="campaign_list_campaigns"),
    re_path(r'^campaigns/list-running/(?P<page>\d+)?$',
        views.campaign_list_running_campaigns,
        name="campaign_list_running_campaigns"),

    re_path(r'^manage/entities$',
        views.admin_entities,
        name="admin_entities"),
    re_path(r'^manage/regions$',
        views.admin_regions,
        name="admin_regions"),
    re_path(r'^manage/users$',
        views.admin_users,
        name="admin_users"),
    re_path(r'^result/click/(?P<targetid>[a-z\d]{8}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{12})$',
        views.campaign_target_click,
        name="campaign_target_click"),
    re_path(r'^result/displayawareness/(?P<campaignid>\d+)/(?P<targetid>[a-z\d]{8}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{12})$',
        views.campaign_display_awareness,
        name="campaign_display_awareness"),
    re_path(r'^result/getimage/(?P<templateid>\d+)/(?P<imgid>img\d+)$',
        views.internet_explorer_img_hack,
        name="getimage"),
    re_path(r'^result/img/(?P<targetid>[a-z\d]{8}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{12})$',
        views.campaign_target_openmail,
        name="campaign_target_openmail"),
    re_path(r'^result/report/(?P<targetid>[a-z\d]{8}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{12})$',
        views.campaign_target_reportmail,
        name="campaign_target_reportmail"),
    re_path(r'^result/attach/(?P<targetid>[a-z\d]{8}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{4}-[a-z\d]{12})$',
        views.campaign_target_openattachment,
        name="campaign_target_openattachment"),
    re_path(r'^2985836c7501af76a0bdd92f1d120cd2/domains_feed',
        views.domains_feed,
        name="domains_feed"),
]
